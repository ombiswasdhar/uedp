import React from 'react';
import './SelectedTrueStatePressedIconFalse.css';

export interface SelectedTrueStatePressedIconFalseProps {
  /** Figma Node ID: 37:691 */
  testId?: string;
}

export const SelectedTrueStatePressedIconFalse: React.FC<SelectedTrueStatePressedIconFalseProps> = ({ testId }) => {
  return (
    <div className="selectedtruestatepressediconfalse-root" data-testid={testId}>
      {/* Component Name: Selected=True, State=Pressed, Icon=False */}
      SelectedTrueStatePressedIconFalse Content
    </div>
  );
};
